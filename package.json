{
  "name": "medinv",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply medinv",
    "db:migrate:remote": "wrangler d1 migrations apply medinv --remote"
  },
  "devDependencies": {
    "wrangler": "^4.2.0"
  }
}

